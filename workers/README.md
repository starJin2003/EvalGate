# workers

Kafka consumer, promotion worker, and judge rescore worker. Lands in P4.

Placeholder in P0 so the layout in `BUILD_PLAN.md` section 6 exists. It becomes a
uv workspace member (its own `pyproject.toml`) in the phase that needs it.
