apiVersion: v1
kind: Service
metadata:
  name: evalgate-api
  namespace: evalgate
  labels:
    app.kubernetes.io/name: evalgate-api
    app.kubernetes.io/part-of: evalgate
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: evalgate-api
  ports:
    - name: http
      port: 80
      targetPort: http
