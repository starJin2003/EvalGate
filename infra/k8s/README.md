# infra/k8s

k3s manifests and helm values for the API, Postgres, model server, kube-prometheus-stack,
and Airflow. Lands in P2 and P3.

Placeholder in P0 so the layout in `BUILD_PLAN.md` section 6 exists.
