# infra/terraform

OCI provisioning: VCN, subnet, security list, one `VM.Standard.A1.Flex` instance on
Ubuntu 24.04 arm64 with cloud-init installing k3s. Lands in P2.

Placeholder in P0 so the layout in `BUILD_PLAN.md` section 6 exists.
