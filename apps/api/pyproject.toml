[project]
name = "evalgate-api"
version = "0.0.1"
description = "EvalGate FastAPI service: suites, runs, diffs, traces, promotion."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/evalgate_api"]
